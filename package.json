{
	"name": "api",
	"version": "1.0.0",
	"description": "\"backend for the persevere app\"",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd client && npm run build",
		"install-client": "cd client && npm install",
		"heroku-postbuild": "npm run install-client && npm run build",
		"start": "node app.js",
		"client": "cd client && npm start"
	},
	"author": "Simon Juli",
	"license": "ISC",
	"dependencies": {
		"async": "^3.2.0",
		"body-parser": "^1.19.0",
		"connect-mongo": "^3.2.0",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-session": "^1.17.1",
		"helmet": "^3.23.3",
		"mongoose": "^5.9.20",
		"nodemailer": "^6.4.10",
		"nodemailer-smtp-transport": "^2.7.4",
		"passport": "^0.4.1",
		"passport-local": "^1.0.0",
		"passport-local-mongoose": "^6.0.1",
		"path": "^0.12.7"
	},
	"engines": {
		"node": "12.18.1"
	}
}
